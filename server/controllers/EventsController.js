// import ApiError from '../error/ApiError.js';
// import EventsService from '../services/EventsService.js';

const attack = async (ws, data) => {
  return true;
};
const message = async (ws, data) => {
  return true;
};
const restore = async (ws, data) => {
  return true;
};
const ability = async (ws, data) => {
  return true;
};

export default {
  restore,
  attack,
  message,
  ability,
};
